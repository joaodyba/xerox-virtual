# xerox-virtual
Ferramenta de captura de tela que detecta automaticamente quando as págins estão totalmente renderizadas antes de capturar, a fim de evitar imagens borradas/incompletas. Inclue seleção de área via interface gráfica (tkinter), navegação gráfica (com OCR) entre páginas e exportação em PDF.
